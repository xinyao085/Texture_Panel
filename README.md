# Texture_Panel
This project is an interactive web-based console designed for the dynamic styling of map regions using SVG patterns. It leverages JavaScript and the D3.js library to render map geometry, manage user interactions, and apply texture fills in real-time.
